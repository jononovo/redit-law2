#!/usr/bin/env node

console.log(`
  shopy — Install shopping skills for AI agents

  Usage:
    npx shopy add <store>        Install a shopping skill
    npx shopy search <query>     Search the skill registry
    npx shopy list               List installed skills
    npx shopy update             Update installed skills

  Learn more: https://shopy.sh/docs/shopy/cli/installation

  Coming soon. This is a placeholder release to reserve the package name.
`);

process.exit(0);
