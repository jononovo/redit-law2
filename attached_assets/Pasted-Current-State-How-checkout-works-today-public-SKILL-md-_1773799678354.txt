Current State
How checkout works today (public SKILL.md + CHECKOUT-GUIDE.md):

The current CHECKOUT-GUIDE.md describes a direct agent flow — the agent itself requests checkout, gets the key, decrypts, fills the form, and confirms. There's no sub-agent lifecycle described.
However, spawn_payload is still sitting in the example JSON response (line 68) with no explanation of what it is or how to use it.
There's also a leftover webhook reference on line 86 (wallet.spend.authorized) that should have been cleaned up when WEBHOOK.md was extracted.
The Security section in SKILL.md mentions "checkout sessions are ephemeral" but doesn't explicitly say "use a sub-agent" — it's vague about who handles the card data.
How checkout used to work (skill-variants/creditcard/dist/encrypted-card.md):

The old variant had a clear, well-structured sub-agent flow:
Steps explicitly labeled "(Sub-Agent Does This)"
A dedicated "Step 2: Spawn a Sub-Agent" section with sessions_spawn() usage
Clear explanation of why sub-agents matter (main agent never sees card data, sub-agent is deleted after)
spawn_payload from the API was the primary mechanism
A fallback note: "If your environment doesn't support spawning sub-agents, you can execute the checkout_steps directly"
The old variant also had a separate encrypted-card.md file dedicated to the Rail 5 flow (card delivery, checkout, status progression) — much cleaner separation
The lib/rail5/index.ts code already supports both modes:

buildCheckoutSteps() — for direct agent execution
buildSpawnPayload() — for sub-agent spawning (returns task, cleanup: "delete", runTimeoutSeconds: 300, label)
The checkout API endpoint returns BOTH in the response
What Was Agreed in the Conversation
The plan you discussed with the previous agent was:

Create an agents/ folder for platform-specific checkout guides
agents/OPENCLAW.md — Clone of CHECKOUT-GUIDE.md but with the full sub-agent lifecycle (spawn → execute → report → confirm → verify deletion). This is the primary path for OpenClaw.
agents/CLAUDE-PLUGIN.md — Placeholder for Claude Desktop/Cowork plugin-based secure checkout
Add a "Secure Card Handoff" table to SKILL.md — routing table right after Payment Rails that tells the agent which path to follow based on their platform
Keep CHECKOUT-GUIDE.md separate — it stays as the platform-agnostic API reference. You'll adapt it later to be the "generic agent" version.
Clean separation of concerns:
agents/ = who handles checkout (platform-specific security model)
checkouts/ = where the checkout happens (merchant-specific form filling)
CHECKOUT-GUIDE.md = what to do (universal API flow)
The last messages clarified: clone CHECKOUT-GUIDE.md into OPENCLAW.md and add the sub-agent/spawn instructions directly in it — don't make one file a wrapper of another. They're separate documents.

What's Missing (Improvements over the old variant)
The old version was missing several things that were discussed:

No report-back step — sub-agent confirms with API but doesn't formally report results to the main agent
No deletion verification — no instruction for the main agent to verify the sub-agent was actually removed
No timeout/failure handling — what happens if the sub-agent hangs or crashes
These would all need to be addressed in the new agents/OPENCLAW.md.

